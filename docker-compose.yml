services:
  nano-rs:
    build: ./docker/nano-rs
    restart: unless-stopped
    # No --config flag: starts with empty registry + admin API on :8889.
    # remo pushes app registrations via admin API after each deploy.
    command: ["nano-rs", "run"]
    environment:
      NANO_ADMIN_API_KEY: "${NANO_ADMIN_API_KEY}"
    ports:
      - "127.0.0.1:8080:8080"
      - "127.0.0.1:8889:8889"
    volumes:
      - /var/lib/remo:/var/lib/remo:ro
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8080/health | grep -q healthy || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 3

  remo:
    build:
      context: .
      args:
        REMO_VERSION: v0.2.5
        REMO_SHA256: 21d8f60d2b054da88611d00cfeb0fa67600dfa4172b20494355014ba4a9c5742
    command: ["remo", "server", "start"]
    environment:
      REMO_NANO_SOCKET: "http://nano-rs:8889"
    restart: unless-stopped
    depends_on:
      nano-rs:
        condition: service_healthy
    ports:
      - "127.0.0.1:7070:7070"
    volumes:
      - /etc/remo:/etc/remo
      - /var/lib/remo:/var/lib/remo
